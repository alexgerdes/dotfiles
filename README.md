# dotfiles
Configuration files for ZSH and Neovim 

The [ZK Context helper](tools/zk-context/README.md) supplies location and weather
for zk notes. Build and install it on each Mac with:

```sh
make -C tools/zk-context install
```
